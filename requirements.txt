# CIR runtime dependencies
#
# Base Docker image:
#   pytorch/pytorch:2.6.0-cuda12.4-cudnn9-runtime
#
# IMPORTANT:
# - Do not install torch/torchvision from this file when using that Docker image.
# - Create the venv with --system-site-packages so it reuses CUDA-enabled
#   torch==2.6.0+cu124 from the base image.

numpy>=1.26,<3
Pillow>=10,<12
PyYAML==6.0.2

# SigLIP2 / Hugging Face
transformers==5.14.1
safetensors>=0.4.5,<1

# Milvus
pymilvus==2.6.17

# API / web viewer
fastapi==0.139.2
uvicorn[standard]>=0.34,<1
jinja2>=3.1,<4
pydantic>=2.10,<3

# OpenAI-compatible VLM HTTP client
httpx==0.28.1