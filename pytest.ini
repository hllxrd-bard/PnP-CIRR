[pytest]
# Collect only the real suite. tmp/ and backups/ hold extracted patch archives
# that carry their own copies of these test modules, which collide on module
# basename during collection.
testpaths = tests
norecursedirs = tmp backups .venv __pycache__ web
